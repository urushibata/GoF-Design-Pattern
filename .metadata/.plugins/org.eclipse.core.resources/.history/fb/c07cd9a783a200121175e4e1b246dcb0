package command.drawer;

import command.command.Command;

/**
 * @author urush_000
 *
 */
public class ColorCommand implements Command {

	// 描画対象
	protected Drawable drawable;
	// 指定色
	private int[] color;

	/**
	 * @param drawable DrawCanvasクラス
	 * @param position 座標
	 * コンストラクタ
	 */
	public ColorCommand(int... colors) {
		this.color = colors;

		System.out.println("追加メンバ変数:R:" + this.color[0] + " G:" + this.color[1] + " B:" + this.color[2]);
		System.out.println("追加メンバ:" + this);
	}

	/**
	 * メンバ変数のdrawable<DrawCanvas>のdrawメソッド実行
	 */
	@Override
	public void execute() {
		drawable.setColor(color);
		System.out.println("実行されるColorCommand:" + Integer.toHexString(this.hashCode()));
	}
}